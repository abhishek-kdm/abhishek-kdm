import { keyframes } from 'styled-components';

export const vintageDisplay = keyframes`
  0% { transform: scaleX(0.1) scaleY(.01); }
  75% { transform: scaleX(1) scaleY(.01); }
  100% { transform: scaleY(1); }
`;
