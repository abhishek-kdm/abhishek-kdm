import styled from 'styled-components';

const Main = styled.main`
    display: flex;
    flex: 1;
    flex-direction: column;

    position: relative;
    overflow: clip;
    padding: 1rem;
`;

export default Main;
